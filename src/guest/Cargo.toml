[package]
name = "guest"
version = "0.1.0"
edition = "2021"

[dependencies]
bnum = { version = "0.11.0", features = ["serde"] }
nexus-rt = { git = "https://github.com/nexus-xyz/nexus-zkvm.git", tag = "0.2.1", version = "0.2.1" }
postcard = { version = "1.0.8", features = ["alloc"] }

# Generated by cargo-nexus, do not remove!
#
# This profile is used for generating proofs, as Nexus VM support for compiler optimizations is still under development.

